<p id="notice"><%= notice %></p>

<h2>
  <strong>Name:</strong>
  <%= @brewery.name %>
</h2>

<p><i>Established in <%= @brewery.year %></i>
</p>

<p>Number of beers: <%= @brewery.beers.count %></p>

<p> <% @brewery.beers.each do |beer| %>
      <%= link_to beer.name, beer %></br>
  <% end %>
</p>

<% if @brewery.ratings.empty? %>
    beers have not yet been rated!</br>
<% else %>
    <p>Has <%= @brewery.ratings.count %>
      <%= "rating".pluralize(@brewery.ratings.count) %>,
      average <%= @brewery.average_rating %> </p>
<% end %>


<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
